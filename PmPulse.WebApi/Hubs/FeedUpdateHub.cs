using Microsoft.AspNetCore.SignalR;

namespace PmPulse.WebApi.Hubs
{
    public class FeedUpdateHub : Hub
    {
        // Clients can connect to this hub and receive feed update notifications
        // The hub handles client connections and disconnections automatically
    }
}
